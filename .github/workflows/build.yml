name: Build and Deploy Snapshot

on:
  push:
    branches:
      - main
    paths-ignore:
      - '**/*.md'
      - '**/*.txt'

permissions: {}

jobs:
  build:
    uses: rosestack/.github/.github/workflows/maven-build.yml@main
    secrets: inherit
